Metadata-Version: 2.4
Name: drfrancintosh
Version: 0.1.0
Summary: Python bindings for the pi02w HDMI RGB565 helpers
Requires-Python: >=3.8
Description-Content-Type: text/markdown

# drfrancintosh

Python bindings for the `hdmi` RGB565 conversion helpers.

Distribution name: `drfrancintosh`
Import module: `hdmi`

## Install

From a local checkout:

```bash
pip install .
```

Or with uv:

```bash
uv pip install .
```

## Use

```python
from hdmi import rgba_to_rgb16, rgb_to_rgb16

rgb565 = rgba_to_rgb16(rgba_bytes, width, height)
```

Both helpers return packed RGB565 bytes for the given image buffer.
